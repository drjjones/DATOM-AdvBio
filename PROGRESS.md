# Site progress

Interactive teaching site for Advanced Biology. This file tracks the site only; the course log is `../PROGRESS.md`.

## 2026-09-17: kickoff, shell, and the carbon allotropes reference build

### Done

- Read the course repo before writing anything. Findings that changed the brief's premises are recorded at the top of the project rules file (kept out of the repository): the textbook is Mader 12e in `../reference/`, not a `./book/` folder; all ten units already have authored standards; the course moved to points-based grading on 2026-09-02, so there are no 0 to 4 descriptors; the parent folder already has a 240 KB `PROGRESS.md`.
- `scripts/extract-content.py` (`npm run extract`) reads the Mader PDF bookmark outline (47 chapters, 194 sections, PDF page = printed + 19) and the ten `UNN_Standards_Student_Copy.docx` / `_Teacher_Copy.docx` files, and writes `content/toc.json` plus `content/standards/unit01.json` to `unit10.json`. Fifty content standards, each with code, sentence-case title, description, "I can" statements, exam blueprint, teacher-copy exam items and points, and its Mader reading (sections, printed pages, PDF pages, section title).
- Astro 7 static site in `site/`: dark by default with a light toggle (remembered per device), presentation mode (`P` key or button: chrome hidden, 20px base type, one `[data-section]` at a time, arrow and page keys, space, swipe, on-screen HUD, `?present` deep link, fullscreen where the browser allows it), `prefers-reduced-motion` honored (rotation starts paused, CSS transitions off), 44px tap targets, system font stack, no runtime CDN calls.
- Offline: `vite-plugin-pwa` wired through a local integration that regenerates the service worker after Astro writes the HTML. Verified after build: the precache manifest lists all 64 pages, every JS and CSS chunk, the manifest, and both icons. Two plugin defaults were wrong for a multi-page site and are overridden: `navigateFallback` (it served the home page for any URL not byte-identical to a precached one, including `?present` deep links; now off) and `ignoreURLParametersMatching` (now ignores every query string so a precached page matches with any parameters).
- Lighthouse, desktop preset, against the production build (`npm run lighthouse` with `npm run preview` running): `/`, `/unit/01/`, `/unit/01/standard/2/`, `/lab/`, `/lab/carbon-allotropes/`, `/teacher/` all score 100 performance, 100 accessibility, 100 best practices. Carbon page: FCP 0.4 s, LCP 0.4 s, TBT 140 ms, CLS 0.001. Note: Lighthouse's Chrome needs classic headless with HTTPS upgrades disabled or it shows an interstitial for http://localhost; the script sets those flags.
- Checked in the desktop app's browser pane at a 1024x768 viewport: gallery renders all six models in one canvas, console clean, `#walkthrough-4` deep link expands C60 with the twelve coral pentagons, theme and play icons toggle correctly, light theme renders, presentation mode hides the chrome and shows the HUD.
- Fixes from that pass, verified in the pane on a cleared cache: presentation mode only hands arrow keys to the walkthrough while the gallery section is the one on screen, and entering presentation with a walkthrough open starts on that section; a plain mouse wheel scrolls the page over an expanded model (Ctrl or Cmd with the wheel, which is what a trackpad pinch sends, zooms), with 44px zoom buttons on the stage; the expanded stage is 44vh tall so the question fits under it at 768px; opening a walkthrough deep link scrolls the stage into view.
- Updates: when a newer build installs behind an older one, a toast offers Reload or Later (verified in the pane: rebuilding while a page was open produced the toast, and Reload brought in the new build). Nothing reloads on its own. Also verified in the pane: tapping an atom shows its label ("Carbon, 4 bonds, sp3") and flashes the atom; the play/pause button toggles rotation. Until the person reloads, the device shows the version it cached, which is the expected trade for working offline.
- Screenshot files at 1024x768 and 1920x1080 in `screenshots/` (`npm run screenshots`, Puppeteer against the preview server): home, carbon gallery, carbon walkthrough step 4, unit 1, standard B1.2, lab index. Headless Chrome's own `--screenshot` flag hangs on the WebGL page, so the script waits a fixed 2.5 s instead.
- Pages: `/` (unit cards in sequence with per-standard progress dots), `/unit/NN/` (five standards as deep-linkable sections with "Present this section" buttons, plus the student-facing exam blueprint), `/unit/NN/standard/K/` (description, tappable "I can" checklist stored on the device, Mader reading with printed and PDF pages, what the exam blueprint says about the standard), `/lab/` (gallery), `/lab/carbon-allotropes/` (reference build), `/teacher/` (presenting notes, exam weight by standard from the teacher copy, per-device progress reset; noindex, not in navigation).
- Reference build `/lab/carbon-allotropes/`: six models generated at runtime from published geometry (`src/lab/carbon/structures.ts`), one WebGL context painting six scissor viewports (`viewer.ts`), drag to rotate, tap an atom for its bond count, expand for full orbit and pinch or wheel zoom, play/pause for rotation, six-step walkthrough with one sentence and one prediction question per step (`walkthrough.ts`), deep links `#model-c60` and `#walkthrough-3`, presentation mode forwards arrow keys to the walkthrough.
- Geometry verified numerically (`node --experimental-strip-types` against `structures.ts`): diamond 87 atoms, every bond 1.545 Å; graphite 176 atoms in three AB-stacked sheets, bonds 1.421 Å, 14 dashed interlayer lines each exactly 3.354 Å; graphene 94 atoms; C60 60 atoms, 90 bonds, all threefold, 12 pentagons covering all 60 atoms, 60 coral pentagon edges and 30 gray; (10,0) tube 140 atoms after trimming the cell-boundary spikes, mean radius 3.917 Å (expected 3.917), bonds 1.416 to 1.421 Å; amorphous 111 atoms, 50 fourfold, 42 threefold, 19 twofold, mean bond 1.527 Å.

### 2026-09-17, later: redesign on the DATOM design language, and still-image fallbacks

Jordan's review of the first build: the models were not visible on the device used, and the style looked generic. Both addressed.

- Redesign on DATOM Design Language v1.1 (tokens verbatim from `DATOM-website/tokens.css`; components from `design-v1.1.css` and the v1.1 spec): letterhead header with the double rule and bordered edges, the DATOM mark as the seal, Spectral display type, Inter body, IBM Plex Mono data labels, filled-ink stamp buttons, hairline ledger tables, leader-dot spec lines, boxed status labels, flat hairline documents, zero radius, no shadows. Fonts self-hosted (nine woff2 files, 164 KB, latin subsets). Two environments from one component set: dark by default for the projector, paper on the toggle, remembered per device.
- Home page is a ledger of ten rows (index, swatch, title, central idea, Mader chapters, five progress squares). Unit pages open each standard with a double rule, the code as a boxed label, and the reading as a mono kicker. Standard pages use a ruled checklist and leader-dot spec lines. Walkthrough answers are bordered rows with a square letter index; feedback carries a signal or contradiction rule.
- Still-image fallbacks: `npm run fallbacks` renders each carbon model to `public/lab/carbon/*.webp` (about 30 KB each) from the built page in `?render=<model>` mode. Every tile shows its still first; the renderer hides it the moment a WebGL context exists. If the browser refuses one, the stills stay, a notice explains, and expand plus walkthrough still work. Verified with `npm run check:no-webgl` (Chrome with 3D APIs disabled): notice shown, stills visible, no script failure.
- Verified: Lighthouse 100 / 100 / 100 on all six route types after the redesign; house-voice sweep clean; console clean in both environments; base-path build resolves fonts and the mark under `/DATOM-AdvBio/`.

### Next

- Jordan settled the three decisions below on 2026-09-17. The reference page itself is live for review; Unit 1 starts once it is approved.
- Jordan reviews the redesign. Then Unit 1 (water), one standard at a time, in the same vocabulary.
- Unit 1 (water), following the unit page spec, one standard at a time.

### Decisions (Jordan, 2026-09-17)

1. **Proficiency ladder.** The spec's 0 to 4 descriptors no longer exist in the course. Decided: the slot shows the standard's "I can" checklist (live on the standard pages); per-standard exam weight appears on the teacher page only.
2. **Unit 7 reading map.** Decided: follow the latest Unit 7 draft. Verified against `U7_Homework_Reading_Schedule_Student.docx` (2026-09-03) and `U7_Session_Plans.docx` (2026-09-04); the one difference from the hand reconciliation was Mader 10.6, which the schedule assigns before the B7.4 session and reuses for B7.5. The JSON now carries both, and the `readingMapDerived` flag is off.
3. **Hosting.** A public link under Jordan's own GitHub account with DATOM in the name, on GitHub Pages; no AI-tool branding on the site, in the repository, or in commits. Done 2026-09-17: repository `drjjones/DATOM-AdvBio` (public), live at https://drjjones.github.io/DATOM-AdvBio/ through the Actions workflow (first deploy run 35293501274). Commits are authored with Jordan's GitHub no-reply address. The site is built with a base path (`SITE_BASE`) so it can live under `/<repository>/`; the workflow in `.github/workflows/deploy.yml` sets it from the repository name.

### Where the book is ambiguous

- Mader folds ATP into section 6.2 and pigments plus light reactions into 7.3, so B3.2 and B3.3, and B5.2 and B5.3, share a section. The crosswalk splits each at a page boundary; the JSON carries the split page ranges.
- Mader has no section on the (10,0) nanotube or fullerenes; the carbon page is a lab resource that supports B1.4 (carbon's four bonds make structural diversity possible) rather than a reading.
